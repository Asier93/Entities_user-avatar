{% extends 'base.html.twig' %}

{% block title %}Avatars{% endblock %}

{% block body %}
    <h1>Avatars</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avatar.id }}</td>
            </tr>
            <tr>
                <th>Character</th>
                <td>{{ avatar.Character }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ avatar.description }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ avatar.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avatars_index') }}">back to list</a>

    <a href="{{ path('app_avatars_edit', {'id': avatar.id}) }}">edit</a>

    {{ include('avatars/_delete_form.html.twig') }}
{% endblock %}
